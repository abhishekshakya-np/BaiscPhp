<?php
// variable
$name = 'Xabickx';
$number = 10; 

// gather and group we need to used array
// this is array
$business == [ 
    'name' => $name,
    'number' => 15
] ;
// to get access to array we need to use key
$business['name'];


// Conditional statement
if ($number ['cost'] > 10) {
    echo "no"   ;
}

//to loop over catagories
$business == [ 
    'name' => $name,
    'number' => 15,
    'catagories' => ["test", "php", "css"]
];

foreach ($business['catagories'] as $category) {
    echo $category;         

}

// function

$business == [ 
    'name' => $name,
    'number' => 15,
    'catagories' => ["test", "php", "css"]
];

function register($user) {
    //crete the user record in the db 
    //log them in 
    //send them a welcome email
    //return them to the homepage or their new dashboard    

}